PORT=3000
NODE_ENV=development
DATABASE_URL=postgresql://postgres:CHANGE_ME@localhost:5432/colorshine_mes
JWT_SECRET=CHANGE_TO_A_LONG_RANDOM_SECRET
CORS_ORIGIN=http://localhost:5173
JWT_EXPIRES_IN=12h
SAP_INBOUND_API_KEY=CHANGE_TO_A_SEPARATE_LONG_RANDOM_INTEGRATION_KEY

# TDC approval e-mail. During UAT every TDC workflow e-mail is redirected here.
# Set TDC_EMAIL_OVERRIDE= (blank) later to send to the active users assigned to the TDC approval groups.
TDC_EMAIL_OVERRIDE=prabakaransara2325@gmail.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
